var game = null;

$(document).ready(initializeApp);

function initializeApp(){
  game = new Gameboard(2);
  game.initializeBoard();
  // intro screen for later
  // ask for number of players
  // create new Gameboard
  // append gameboard
  // start game
}
